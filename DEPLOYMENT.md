# نشر التطبيق على Vercel

هذا الدليل يشرح كيفية نشر لعبة "أبطال السوشال ميديا" على Vercel.

## التحضيرات

التطبيق الآن أصبح **static frontend فقط** بدون backend - كل البيانات والمنطق موجودة في الـ frontend.

### التغييرات المهمة:

1. **البيانات المحلية**: تم نقل جميع الأسئلة من `server/storage.ts` إلى `client/src/data/questions.ts`
2. **لا يوجد API calls**: تم استبدال جميع استدعاءات الـ API بوظائف محلية
3. **Build بسيط**: الـ build الآن ينتج ملفات static HTML/CSS/JS فقط

## خطوات النشر على Vercel

### 1. Push الكود على GitHub

```bash
git add .
git commit -m "Convert to static app for Vercel deployment"
git push origin main
```

### 2. ربط المشروع بـ Vercel

1. اذهب إلى [vercel.com](https://vercel.com)
2. سجل دخول أو أنشئ حساب جديد
3. اضغط على **"Add New Project"**
4. اختر الريبو من GitHub
5. Vercel سيكتشف تلقائياً أنه Vite project

### 3. إعدادات Build

Vercel سيستخدم الإعدادات من `vercel.json` تلقائياً:

- **Framework Preset**: Vite (أو Other)
- **Build Command**: `vite build` ✅ (مهم جداً - استخدم هذا وليس npm run build)
- **Output Directory**: `dist/public` ✅ (مهم - Vite يبني في dist/public)
- **Install Command**: `npm install`

**ملاحظة مهمة**: الـ `vercel.json` يحتوي على الإعدادات الصحيحة:
- `"buildCommand": "vite build"` لضمان بناء الـ frontend فقط
- `"outputDirectory": "dist/public"` لأن Vite يبني الملفات في هذا المجلد

### 4. Deploy!

اضغط على **"Deploy"** وانتظر حتى ينتهي البناء (~2-3 دقائق)

## ملاحظات مهمة

### ✅ ما تم تحويله:

- ✅ جميع الأسئلة الـ 10 موجودة محلياً
- ✅ نظام الأصوات التفاعلية (Web Audio API)
- ✅ Shuffle الأسئلة عشوائياً
- ✅ التحقق من الإجابات محلياً
- ✅ نظام النقاط والتقدم
- ✅ Fullscreen mode
- ✅ Mute/Unmute sounds

### 🎨 المميزات المحفوظة:

- الأصوات التفاعلية تعمل بشكل كامل
- جميع الرسوم المتحركة (Framer Motion)
- التصميم الكامل بالعربي RTL
- شاشة الترحيب، الأسئلة، الـ feedback، والشاشة النهائية
- نظام النجوم الأخضر

### ⚡ الأداء:

- تحميل فوري (لا يوجد API calls)
- لا يوجد latency للأسئلة
- يعمل بدون اتصال بالإنترنت (بعد أول تحميل)

## إضافة أسئلة جديدة

لإضافة أسئلة جديدة، عدّل الملف:
```
client/src/data/questions.ts
```

اتبع نفس الصيغة الموجودة:

```typescript
{
  id: 11,  // رقم متسلسل جديد
  scenario: "السؤال بالعربي؟",
  choice1: "الخيار الأول",
  choice2: "الخيار الثاني",
  choice3: "الخيار الثالث",
  choice4: "الخيار الرابع",
  correctAnswer: 2,  // رقم الإجابة الصحيحة (1-4)
  tip: "نصيحة تعليمية للأطفال",
  category: "passwords",  // أو strangers, sharing, privacy
}
```

بعد التعديل، اعمل push للكود - Vercel سيعيد النشر تلقائياً!

## الـ Domain المخصص (اختياري)

بعد النشر، يمكنك ربط domain مخصص من إعدادات المشروع في Vercel:

1. اذهب إلى **Settings** > **Domains**
2. أضف الـ domain الخاص بك
3. اتبع التعليمات لتحديث DNS records

## استكشاف الأخطاء

### المشكلة: الصفحة فارغة بعد الـ deployment
**الحل**: تأكد من أن `vercel.json` موجود ويحتوي على rewrites للـ SPA routing

### المشكلة: الأصوات لا تعمل
**الحل**: الأصوات تحتاج تفاعل من المستخدم أولاً (click) - هذا سلوك طبيعي في المتصفحات

### المشكلة: الخطوط العربية لا تظهر
**الحل**: تأكد من وجود الاتصال بـ Google Fonts في `index.html`

## الدعم

للمساعدة أو الأسئلة، راجع:
- [Vercel Documentation](https://vercel.com/docs)
- [Vite Documentation](https://vitejs.dev)
